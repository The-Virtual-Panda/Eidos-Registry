# Eidos-Registry
The registry of published Eidos frameworks: one manifest per package pointing at a root in its author's repository at a tag. Publish with a pull request; CI validates, a merge makes it live.
