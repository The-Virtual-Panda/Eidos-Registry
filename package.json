{
  "name": "eidos-registry",
  "private": true,
  "type": "module",
  "description": "The registry of published Eidos frameworks: one manifest per package.",
  "license": "Apache-2.0",
  "scripts": {
    "validate": "node scripts/validate.mjs"
  },
  "devDependencies": {
    "yaml": "^2.8.0"
  }
}
